# Python backend

This directory will contain the FastAPI application and every server-side domain, data, background-job, and AI capability.

Phase 0 Step 2 will establish the `src/galaxy_frog` package and Python quality tooling. Later phases must keep provider SDKs behind typed adapters.
