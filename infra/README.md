# Infrastructure

Local development infrastructure belongs here. Phase 0 Step 3 adds PostgreSQL/pgvector configuration while the root Compose file provides the developer entry point.

AWS infrastructure is deliberately deferred until the local architecture and costs have been measured.
