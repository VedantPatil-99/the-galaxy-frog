# Applications

`apps/web` will contain the presentation-only Next.js application beginning in Phase 0 Step 2.

No database access, AI provider SDK, ingestion pipeline, or retrieval logic belongs under `apps`.
